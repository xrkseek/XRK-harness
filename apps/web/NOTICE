# NOTICE

## XRK Harness Web

产品聊天壳以 **DeepSeek Harness** Web（MIT，Copyright DeepSeek）为底稿做二次创作。许可全文见上游 MIT License，须随拷贝保留。

这不是 GitHub Fork：不跟踪 DSH 默认分支，不向 deepseek-ai 仓库提 PR。后续改动只进入本仓（XRK-Harness / `@xrkseek`）。

当前仓库托管的是 Face 验证台（本目录）与本机捕获静态壳（`apps/web-static`，gitignore）。源码底稿入库后仍用本 NOTICE。

品牌：`docs/assets/logo*.png`（栅格，不重绘 SVG）。
