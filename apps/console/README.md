# @xrkseek/harness-console

Face 接线验证台（`?console=1`）。维护者工具，**不是**产品聊天 UI。

产品入口：编出 [`apps/web`](../web/) + [`packages/client`](../../packages/client/) → `apps/web/dist`，再 `xrk-harness serve` / `web`。
