# presets/harness

Composition only: fs + bash + std tools · sandbox guards · workspace inject · optional `run_code`.

`workspaceInject` / `slashRecipes` default on with assemble — see `docs/workspace-inject.md`, `docs/slash-recipes.md`.
