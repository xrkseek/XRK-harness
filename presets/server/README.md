# presets/server

Host 平面 composition：提供 `createServerAgentFactory` / `createServerComposition`，供 `serve` 把 HTTP 绑到 harness（或可切换）session 组合。

无业务逻辑。见 [docs/host-preset.md](../../docs/host-preset.md) · [docs/profiles.md](../../docs/profiles.md)。
