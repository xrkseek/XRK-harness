# @xrkseek/core-tools

Tool registry + **waterfall pipeline**：

`pre → guards → execute → post → finalize → bound → freeze result`

另含：`materializeTools` · `boundToolOutput` · `createStdTools` · write-intent / policy denylist guards。

规格：[docs/tool-pipeline.md](../../../docs/tool-pipeline.md) · [docs/tool-output-bound.md](../../../docs/tool-output-bound.md)
