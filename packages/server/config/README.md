# @xrkseek/server-config

`loadHostConfig`：defaults < env < patch。密钥只从 env 读。

含可选 `XRK_PLUGINS_DIR` / `runtime.pluginsDir`。  
见 [docs/http-api.md](../../docs/http-api.md) · [docs/plugin-loader.md](../../docs/plugin-loader.md)。
