# @xrkseek/exec-shell

`ShellService` over `@xrkseek/exec-subprocess` (bash / cmd / pwsh).

- `run` — foreground  
- `startJob` / `listJobs` / `killJob` — in-process background jobs  
- `createBashTools` → `bash` · `bash_jobs` · `bash_kill`

See `docs/shell-jobs.md` · `docs/seams.md`.
