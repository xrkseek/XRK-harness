# Temporary stubs

Empty workspace packages so `pnpm install` can resolve `@deepseek-ai/dsh-*` names the shell still declares. Not product APIs — replace with real wire packages or remaps as self-build lands.

See apps/web/NOTICE · packages/cordis/README.md.
