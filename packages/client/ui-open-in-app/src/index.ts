/**
 * Session-header "Open in app" plugin — node half. Empty apply keeps the
 * package on the Host roster; the browser half ships via `./client`.
 */

/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply(): void {}
