/**
 * Interchange format generation (role JSONL header `xrkInterchange`).
 * Kept numeric-only here so `@xrkseek/session-format` has no core-session cycle.
 */
export const SESSION_INTERCHANGE_VERSION = 1;
