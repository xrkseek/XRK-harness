# 要义摘录 / Learn

> **读者 / Audience**：集成者 · 贡献者 · 维护者 / Integrators · Contributors · Maintainers

已落地能力的要义摘录；细节见各专题与 [modules/](./modules/README.md)。身份路径：[audiences.md](./audiences.md)。

Short digest of shipped capabilities; details live in topic docs and [modules/](./modules/README.md). Audience paths: [audiences.md](./audiences.md).

- **Session 事件为对话真源 / Session events are the conversation source of truth**：模型可见输入可从事件日志重建；turn / loop 短寿。 / Model-visible input is reconstructible from the event log; turn / loop are short-lived.

- **Session 仓 / Session store**：`createPersistentSessionStore`（`node:sqlite` · `sessions.db` · WAL · **schema v3**：scalar + 物理 `text-chunks` / `tool-call-chunks`（≥3 chunk 落库打包）+ FTS5 trigram · **lazy load** · **chunk 写批** · **`flush()`** · **open-turn 崩溃修复**）；Host 无 `XRK_SESSIONS_DIR` 时内存。CLI `serve`/`web` 默认 `~/.xrk/sessions`（`--no-persist` 关）。Host drain idle / `stop` 时 `flush`/`close`。ZIP 导出用 `toPackedJSONL`（`text-chunks` · `tool-call-chunks`）+ **`.jsonl.zst` sidecar**（明文 `.jsonl` 仍保留）；`fromPackedJSONL` / `parsePackedJSONL` 可导入。CLI dump 仍 `toJSONL`。 / `createPersistentSessionStore` (`node:sqlite` · `sessions.db` · WAL · **schema v3**: scalars + physical `text-chunks` / `tool-call-chunks` (pack to disk at ≥3 chunks) + FTS5 trigram · **lazy load** · **chunk write batching** · **`flush()`** · **open-turn crash repair`). Host without `XRK_SESSIONS_DIR` is in-memory. CLI `serve`/`web` default to `~/.xrk/sessions` (`--no-persist` disables). Host `flush`/`close` on drain idle / `stop`. ZIP export uses `toPackedJSONL` (`text-chunks` · `tool-call-chunks`) + **`.jsonl.zst` sidecar** (plain `.jsonl` retained); import via `fromPackedJSONL` / `parsePackedJSONL`. CLI dump still uses `toJSONL`.

- **工具瀑布 / Tool waterfall**：pre → guards → execute → post → finalize → settle；无全局 proxy。`todo_write` / `exit_plan_mode` 经 `emitToolEvent` 入账 `todo/write` · `plan/mode`（不进 `deriveMessages`）。 / pre → guards → execute → post → finalize → settle; no global proxy. `todo_write` / `exit_plan_mode` are booked via `emitToolEvent` as `todo/write` · `plan/mode` (not into `deriveMessages`).

- **投影 / Projection**（`@xrkseek/session-projection` 状态/视图分离 / state/view split）：Face 默认 `title` · `sessionListMetadata` · `todos` · `permissions` · `plan` · **`sessionStats`** · **`tokenUsage`** · **`contextPressure`** · **`contextBreakdown`**（StatsLine / ContextMeter）。Provider usage 入事件后可累计；压力结合 usage 与 surface 估算；`context/compaction.shadowedTokenCount` 可缩小 meter 窗口（缺字段不缩小）。压缩预算用 `estimateTokens`，与 meter 估算分开。详见 [session-compaction.md](./session-compaction.md) · [modules/session-projection.md](./modules/session-projection.md) · [protocol-events.md](./protocol-events.md)。 / Face defaults include those fields for StatsLine / ContextMeter. Provider usage accumulates after it enters events; pressure combines usage and surface estimates; `context/compaction.shadowedTokenCount` can shrink the meter window (missing field does not shrink). Compaction budget uses `estimateTokens`, separate from meter estimates. See the linked docs.

- **权限预设 / Permission presets**：`session.create` 按 settings `permission.defaultPreset`（缺省 `workspace-write`）钉 knobs；`/permission` 切换；`read-only` → policy deny 写/shell；`danger-full-access` → 审批 `never` 自动放行且不挂 workspace sandbox wrap；`workspace-write` 维持现有 confine。 / `session.create` pins knobs from settings `permission.defaultPreset` (default `workspace-write`); `/permission` switches; `read-only` policy-denies write/shell; `danger-full-access` auto-approves with `never` and skips workspace sandbox wrap; `workspace-write` keeps the existing confine.

- **Plan mode**：`/plan` · `/plan off` · `/plan <message>`（suffix steer）；idle 立即钉 `plan/mode`，开 turn 则下一 step 提交。`exit_plan_mode` 常驻工具表；仅 active 时走 plan-review 提问，批准后 side-event 退出。软引导，不改沙箱/审批。 / `/plan` · `/plan off` · `/plan <message>` (suffix steer); idle pins `plan/mode` immediately, an open turn commits on the next step. `exit_plan_mode` stays on the tool table; plan-review questions only when active, then exit via side-event after approval. Soft guidance; does not change sandbox/approval.

- **能力缝 / Capability seams**：Definition / Provider / Consumer。`@xrkseek/exec-web` 登记 `web_search` / `web_fetch`（harness/server）；默认 **parallel-free → duckduckgo**（对齐 XRK-AGT），有 `XRK_TAVILY_*` / Brave 则优先。`tool/result.meta` 供 `card: "web"` 冷回放。`skill` 工具自动导入已存在的 `.claude|.cursor|.agents|.codex|.xrk/skills`（及用户主目录同名；与 Face `skill.list` 同源；不强制建目录）。工作区站立注入：turn 开头持久 `user/message`（`skill-catalog` · `agent-instructions`，聊天「上下文注入」可见；不进 system）。用户消息 `/skill-name` 把 `<skill_content>` 写入 `user/message`（可重建；recipe 同名优先）。`lsp` 工具四操作导航（harness/server）；无 `XRK_LSP_COMMAND` 时仍可见、execute 诚实失败。**`terminal_*` 六件套**是真 PTY（`node-pty@1.2.0-beta.15` NAPI prebuild · `name:dumb` · env scrub · host-exit 杀树 · `hasActivity` 沙箱 fence · 六件套 `result.meta` + `run_in_background`→`pty-send`/`outputLimitBytes`；Win 为 no-op inspector）；无 native 时仍可见、`terminal_open` 诚实失败。后台 job：`job_list` / `job_output`（`statusLine`+detail）/ `job_kill` + Face settle 完成通知（idle wake / busy inject / 预算 3；`reported` 跳过重复）；不是产品编辑器。 / Definition / Provider / Consumer. `@xrkseek/exec-web` registers `web_search` / `web_fetch` (harness/server); default **parallel-free → duckduckgo** (aligned with XRK-AGT), with `XRK_TAVILY_*` / Brave preferred when present. `tool/result.meta` feeds `card: "web"` cold replay. The `skill` tool auto-imports existing `.claude|.cursor|.agents|.codex|.xrk/skills` (and same names under the user home; same source as Face `skill.list`; does not force-create directories). Standing workspace inject: durable `user/message` at turn start (`skill-catalog` · `agent-instructions`, visible as chat “context inject”; not into system). User `/skill-name` writes `<skill_content>` into `user/message` (reconstructible; same-named recipe wins). `lsp` exposes four navigation operations (harness/server); still visible without `XRK_LSP_COMMAND`, execute fails honestly. The **`terminal_*` six-pack** is a real PTY (`node-pty@1.2.0-beta.15` NAPI prebuild · `name:dumb` · env scrub · host-exit kills the tree · `hasActivity` sandbox fence · six-pack `result.meta` + `run_in_background`→`pty-send`/`outputLimitBytes`; Win is a no-op inspector); still visible without native, `terminal_open` fails honestly. Background jobs: `job_list` / `job_output` (`statusLine`+detail) / `job_kill` plus Face settle completion notices (idle wake / busy inject / budget 3; `reported` skips duplicates); not a product editor.

- **Host Face**：Unary RPC + mux/host 双流；未实现方法 `not-implemented`。 / Unary RPC + mux/host dual streams; unimplemented methods return `not-implemented`.

- **Host 流 / Host streams**：`host/session-status` 由 drain latch settle 推送（DSH `agent/status` 同形态；`session.prompt` 不 poll）。`host/session-added` 含 `sessionListFields`（子会话 `parentSessionId` + `origin: "subagent"`；`blank` = 无 `turn/start`）。`workspace.delete` → `host/workspace-removed`；`insertBefore` → `host/workspace-order-changed`。凭据 / 设置 / preset 真改发 `host/remote-event`（`credentials/updated` · `settings/document-updated` · `agent-preset/selected` · `llm/adapters-updated`）；不转发 Cordis inspect/run。无 dispose，不发 `host/session-removed`。 / `host/session-status` is pushed on drain latch settle (same shape as DSH `agent/status`; `session.prompt` does not poll). `host/session-added` includes `sessionListFields` (child sessions carry `parentSessionId` + `origin: "subagent"`; `blank` = no `turn/start`). `workspace.delete` → `host/workspace-removed`; `insertBefore` → `host/workspace-order-changed`. Real credential / settings / preset changes emit `host/remote-event` (`credentials/updated` · `settings/document-updated` · `agent-preset/selected` · `llm/adapters-updated`); Cordis inspect/run is not forwarded. No dispose path; `host/session-removed` is not emitted.

- **Session cwd**：`session.create({ workspaceId })` 钉 `sessionCwds`；Host agent factory 用会话 cwd（非仅 serve `--workspace` 根）。Glob/bash 读会话 cwd；skill 多根导入；工作区根 `AGENTS.md` · `.cursor/rules` 等经 `agent-instructions` 注入（见 [workspace-inject](./workspace-inject.md)）。 / `session.create({ workspaceId })` pins `sessionCwds`; the Host agent factory uses the session cwd (not only the serve `--workspace` root). Glob/bash read the session cwd; skill multi-root import; workspace-root `AGENTS.md` and `.cursor/rules` inject via `agent-instructions` ([workspace-inject](./workspace-inject.md)).

- **Face wire / 历史 / Face wire / history**：`turn/end` wire 必带 `reason`（协议必填）。`session.history` 按 **message 边界**分页（`user/message` + `assistant/message`，默认 50），chunk/turn 脚手架跟消息组一起走，不是按原始事件条数切页。 / `turn/end` wire must include `reason` (protocol-required). `session.history` paginates on **message boundaries** (`user/message` + `assistant/message`, default 50); chunk/turn scaffolding travels with the message group, not by raw event count.

- **取消流式 / Cancel streaming**：abort 时把已落库 `assistant/chunk` 固化为 `assistant/message`（`interrupted: true`），再关 step/turn；壳 assistant 节点读 `data.interrupted`。 / On abort, persisted `assistant/chunk` events are solidified into `assistant/message` (`interrupted: true`) before closing step/turn; the shell assistant node reads `data.interrupted`.

- **会话归档 / Session archive**：侧栏会话行 `⋯` →「归档会话」（`workspace.archiveSession`）；从列表隐藏、**不删** SQLite 日志（DSH 同语义，无硬删除按钮）。 / Sidebar session row `⋯` → “Archive session” (`workspace.archiveSession`); hides from the list and **does not delete** the SQLite log (same semantics as DSH; no hard-delete button).

- **组合叶 / Compose leaf**：`@xrkseek/compose` C0·C1·C2（`interceptInject` / `openSubagentRealm`）能跑；presets 只接线。 / `@xrkseek/compose` C0·C1·C2 (`interceptInject` / `openSubagentRealm`) work; presets only wire.

- **Subagent**：Face `list/history/prompt/interrupt` 能跑；`session.create({ parentSessionId })` 或 `session.fork` 登记子会话。Host agent-cache 对有父的会话走 `openSubagentRealm`（无 ACP / Cordis 外挂）。 / Face `list/history/prompt/interrupt` work; `session.create({ parentSessionId })` or `session.fork` registers a child session. Host agent-cache uses `openSubagentRealm` for parented sessions (no ACP / Cordis external attach).

- **进程插件 / Process plugins**：`tools` · `prompt` · `commands` 已接线；`channel` / `policy` / `llm` 可发现、未自动接线；`kind: cordis` 只 discover；社区 client 经 `dsh-compat` bridge（见 [community-plugins.md](./community-plugins.md)）。 / `tools` · `prompt` · `commands` are wired; `channel` / `policy` / `llm` are discoverable but not auto-wired; `kind: cordis` is discover-only; community clients bridge via `dsh-compat` ([community-plugins.md](./community-plugins.md)).

- **队列 / Queue**：权威快照 `session/queue`；`prompt/*` → `agent/inbox/spliced`；mux 重连可补发 pending queue。 / Authoritative snapshot is `session/queue`; `prompt/*` → `agent/inbox/spliced`; mux reconnect may re-send a pending queue.

- **提问 / Questions**：mux `question/requested`（rpcId = 问题 id）→ `/api/respond`（先审批后提问；`cancelled` 取消）。`ask_user` 支持自由文本 `question` 与 DSH 形 `questions[]`（options / multi_select / `intent.kind: plan-review`）；Face / Host bind 到 broker；mux 重连补 pending。无 UI 时工具仍可回 isError。 / mux `question/requested` (rpcId = question id) → `/api/respond` (approval before question; `cancelled` cancels). `ask_user` supports free-text `question` and DSH-shaped `questions[]` (options / multi_select / `intent.kind: plan-review`); Face / Host bind to the broker; mux reconnect replenishes pending. Without UI the tool may still return isError.

- **后台任务 / Background jobs**：有 jobs 源才发 `session/jobs`；订阅基线只在集合非空时发（空集靠缺帧）；变更推全量快照（清空仍发 `[]`）。无主任务扇出到每个已列出会话。`JobView` 不含 `ownerSession` / `reported` / `outputLimitBytes`。Jobs 内核：`running`→`stopping`→终态、`bash-N` / `<kind>-N`、按 owner 并发上限 10、`dispose` teardown 置 `reported`；Host 共享 root + `createSessionScopedShell` 隔离（可见集未变则不转发 `onJobsChanged`）。 / Emit `session/jobs` only when a jobs source exists; subscription baseline only when the set is non-empty (empty relies on a missing frame); changes push a full snapshot (clear still sends `[]`). No fan-out of a main job to every listed session. `JobView` omits `ownerSession` / `reported` / `outputLimitBytes`. Jobs kernel: `running`→`stopping`→terminal, `bash-N` / `<kind>-N`, per-owner concurrency cap 10, `dispose` teardown sets `reported`; Host shares a root plus `createSessionScopedShell` isolation (no `onJobsChanged` forward when the visible set is unchanged).

- **产品壳 / Product shell**：`apps/web` + `packages/client`（client 定点拷贝基线 `dsh-v0.1.0-rc.8`；对照真源 `XRKbar/deepseek-harness` · [UPSTREAM](../apps/web/UPSTREAM) 跟踪 `dsh-v0.1.1-rc.2`）。**rc.2 已对齐（定点）**：`ui-attachment` slot 插件（`conversation.input.attachments` / `message.images`，经 `boot.json` 加载，不进 platform seed）· `ui-conversation`（InputBar 走 attachment slots；保留 `image-limits-projection`）· `ui-subagent`（`header.lineage` + read-only composer；`@` 已迁至 `ui-reference`）。**rc.2 已搬（context + Face 发现）**：`ui-reference`（统一 `@file` / `@session` source）· `packages/context/{file-reference,file-reference-local,session-reference}` · Face `fileReferences/list` + `sessionReferenceResolver/candidates`（元数据发现；**prepare 注入**仍依赖 Cordis `sessionQuery`，Face 主路径未接）。**产品自研替代（不跟上游同名包）**：`ui-renderer`（XRK 用 `web-react`）· `ui-brand-official`（自有 `BrandMark`）。npm 已 rescoped：`@xrkseek/web-frontend` · `@xrkseek/client-*` · stubs `@xrkseek/xrk-*` · Cordis 薄栈 `@xrkseek/cordis*`。browser 类型链依赖 stubs 的 `./types` / `./client`（如 `xrk-user-questions` · `xrk-user-approval` · `xrk-subagent` · `xrk-host-webserver`）；`pnpm client:types` 发 `lib/types`（`emit-client-types-all` 兼发无 `xrk.client` 的包与 connection host）。boot 注入 `window.__XRK_BOOT__`。插件清单读各包 `package.json` 的 `xrk.client`（不是 `dsh.client`）。`pnpm web:build` + `pnpm client:bundle` + `pnpm web:assemble` → `apps/web/dist`（37 条：含 `client-ui-reference` · `client-session-log-export` + Face 立即层 `xrk-typert-registry` · `xrk-api-gateway` · `xrk-api-remotes`；omit HMR / Cordis UI / native picker）。规格见 [modules/references.md](./modules/references.md)。Host `product-shell.test` 有完整 dist 才跑：GET `/` · boot 注入（含三件 Face 立即层）· plugin 200。 / `apps/web` + `packages/client` (client pin baseline `dsh-v0.1.0-rc.8`; reference tree `XRKbar/deepseek-harness` · [UPSTREAM](../apps/web/UPSTREAM) tracks `dsh-v0.1.1-rc.2`). **rc.2 aligned (pin)**: `ui-attachment` slot plugin (`conversation.input.attachments` / `message.images`, loaded via `boot.json`, not platform seed) · `ui-conversation` (InputBar uses attachment slots; keeps `image-limits-projection`) · `ui-subagent` (`header.lineage` + read-only composer; `@` moved to `ui-reference`). **rc.2 ported (context + Face discovery)**: `ui-reference` (unified `@file` / `@session` source) · `packages/context/{file-reference,file-reference-local,session-reference}` · Face `fileReferences/list` + `sessionReferenceResolver/candidates` (metadata discovery; **prepare inject** still depends on Cordis `sessionQuery`, not on the Face main path). **First-party alternatives (not same-named upstream packages)**: `ui-renderer` (XRK uses `web-react`) · `ui-brand-official` (own `BrandMark`). npm rescoped to `@xrkseek/web-frontend` · `@xrkseek/client-*` · stubs `@xrkseek/xrk-*` · thin Cordis stack `@xrkseek/cordis*`. Browser type chain depends on stub `./types` / `./client` (e.g. `xrk-user-questions` · `xrk-user-approval` · `xrk-subagent` · `xrk-host-webserver`); `pnpm client:types` emits `lib/types` (`emit-client-types-all` also covers packages without `xrk.client` and the connection host). Boot injects `window.__XRK_BOOT__`. Plugin manifests read `xrk.client` from each package’s `package.json` (not `dsh.client`). `pnpm web:build` + `pnpm client:bundle` + `pnpm web:assemble` → `apps/web/dist` (37 entries: including `client-ui-reference` · `client-session-log-export` + Face immediate layer `xrk-typert-registry` · `xrk-api-gateway` · `xrk-api-remotes`; omit HMR / Cordis UI / native picker). Spec: [modules/references.md](./modules/references.md). Host `product-shell.test` runs only with a complete dist: GET `/` · boot inject (including the three Face immediate-layer packages) · plugin 200.

- **内核对齐（已跟对照版） / Kernel alignment (matched to the reference cut)**：`session.history` message 边界分页（50）；取消流式前缀固化；DeepSeek V4 默认模型；SQLite 持久化（**schema v3** 物理 `text-chunks` · lazy · batch · flush · repair）；ZIP 导出 `text-chunks` + zstd sidecar；`maxRequestImageBytes` 请求级图片 offload（20MiB）；HTTP **413** 请求体过大 → `UnsupportedContentError`；`web_search` 并发 `queries[]`；**cancel e2e**（`product-shell-cancel.e2e.ts`）。**产品差异 / 不适用**：Codex/Claude Profile Bundle；DeepSeek **官方** Flash/Pro 仍 text-only；`deepseek-v4-flash-vision-exp` 标 `text+image`（自定义 gateway 默认可声明视觉）；**client rc.2 定点 resync 已完成**（见上条「产品壳」）；Cordis scaffold 全家桶；多事件 `compaction/summary|prune` + surfaceOp replace（XRK 用单事件 `context/compaction.shadowedTokenCount` 等价缩窗口）。 / Matched: `session.history` message-boundary pagination (50); cancel-stream prefix solidify; DeepSeek V4 default models; SQLite persistence (**schema v3** physical `text-chunks` · lazy · batch · flush · repair); ZIP export `text-chunks` + zstd sidecar; `maxRequestImageBytes` request-level image offload (20MiB); HTTP **413** oversized body → `UnsupportedContentError`; `web_search` concurrent `queries[]`; **cancel e2e** (`product-shell-cancel.e2e.ts`). **Product differences / not applicable**: Codex/Claude Profile Bundle; official DeepSeek Flash/Pro remain text-only; `deepseek-v4-flash-vision-exp` is marked `text+image` (custom gateways may declare vision by default); **client rc.2 pin resync is done** (see Product shell above); full Cordis scaffold suite; multi-event `compaction/summary|prune` + surfaceOp replace (XRK equivalently shrinks the window with a single `context/compaction.shadowedTokenCount` event).

- **附件 / 视觉 / Attachments / vision**：`MessageContent` 可为块数组；`@xrkseek/attachment`；Face `session.attachment`；Host Face **intake** 默认 `text+image`（InputBar）。**Live 适配器 modality 以 Registry 为准**（官方 Flash/Pro text-only；`deepseek-v4-flash-vision-exp` 与自定义 gateway 可 `text+image`）——`resolveLlmForSelection` 不再把 Face intake 盖到 adapter 上。`session.prompt` / MCP `imageAdmission` 经 `liveRouteAllowsImageInput` 双闸（Face intake + live route）。openai-compatible 含 `image` 才走 `image_url` data URL。MCP 工具结果：有序块投影；准入成功时 `tool/result.content` 为 ContentBlock[]，否则 diagnostic（不 dump base64）。 / `MessageContent` may be a block array; `@xrkseek/attachment`; Face `session.attachment`; Host Face **intake** defaults to `text+image` (InputBar). **Live adapter modality follows the Registry** (official Flash/Pro text-only; `deepseek-v4-flash-vision-exp` and custom gateways may be `text+image`) — `resolveLlmForSelection` no longer overlays Face intake onto the adapter. `session.prompt` / MCP `imageAdmission` use the `liveRouteAllowsImageInput` dual gate (Face intake + live route). openai-compatible emits `image_url` data URLs only when `image` is present. MCP tool results: ordered block projection; on successful admission `tool/result.content` is ContentBlock[], otherwise a diagnostic (no base64 dump).

- **LLM 流 / LLM streaming**：OpenAI 兼容适配器默认 `stream()`：SSE `reasoning_content` → `reasoning-delta`（index 0）、`content` → `text-delta`（index 1）、`tool_calls` → `tool-call-delta` → session `assistant/chunk` kind=`tool-call`；流请求带 `stream_options.include_usage`；SSE `usage` → mid-stream `LlmStreamEvent` `usage` → session `assistant/chunk` kind=`usage`，再 `done`/chat JSON → `assistant/message.usage`。agent-loop 先 append chunk 再 message（可选 `reasoning` · `usage`）。`chat()` 仍一次性 JSON。DeepSeek modality 见 catalog（vision-exp 标视觉）。**AI 路径（已对齐对照版 `dsh-v0.1.1-rc.2`）**：harness/server 默认 `maxSteps=32`；Goal 默认 16 轮；每轮 reasoning 回传；`length` keep/drop → `max-tokens`；`EMPTY_RESPONSE` / 未知 finish / 残缺 tool JSON → `turn/end` error；derive 跳过 usage-only 空 assistant；Face `reasoningEffort` → DeepSeek `thinking`/`reasoning_effort`；软压 prune + soft compact；overflow 先 prune；`toolOrder`；Anthropic cache_control + cache usage；OpenAI/Anthropic HTTP → `LlmError` 码（`AUTH`/`RATE_LIMIT`/`SERVER`/…）；步内 `llm/retry`（默认可重试 `EMPTY_RESPONSE`/`RATE_LIMIT`/`SERVER`/`TIMEOUT`/`TRANSPORT`）；崩溃结算 `TOOL_NOT_STARTED` / `TOOL_OUTCOME_UNKNOWN`；**`isConcurrencySafe` settle** · `extras.concludeTurn`/`deferContext` · `AgentCancelCause` · vision-exp catalog · 同轮 retry 后 turn-error；SQLite/`toPackedJSONL` **`tool-call-chunks`**。 / OpenAI-compatible adapters default to `stream()` with the SSE → session chunk mapping above; stream requests include `stream_options.include_usage`; SSE `usage` becomes mid-stream `LlmStreamEvent` `usage` then session `assistant/chunk` kind=`usage`, then `done`/chat JSON → `assistant/message.usage`. The agent-loop appends chunks before the message (optional `reasoning` · `usage`). `chat()` remains one-shot JSON. DeepSeek modality is in the catalog (vision-exp marked visual). **AI path (aligned to reference cut `dsh-v0.1.1-rc.2`)**: harness/server default `maxSteps=32`; Goal default 16 rounds; per-round reasoning echo; `length` keep/drop → `max-tokens`; `EMPTY_RESPONSE` / unknown finish / broken tool JSON → `turn/end` error; derive skips usage-only empty assistants; Face `reasoningEffort` → DeepSeek `thinking`/`reasoning_effort`; soft-pressure prune + soft compact; overflow prunes first; `toolOrder`; Anthropic cache_control + cache usage; OpenAI/Anthropic HTTP → `LlmError` codes (`AUTH`/`RATE_LIMIT`/`SERVER`/…); in-step `llm/retry` (default-retryable `EMPTY_RESPONSE`/`RATE_LIMIT`/`SERVER`/`TIMEOUT`/`TRANSPORT`); crash settlement `TOOL_NOT_STARTED` / `TOOL_OUTCOME_UNKNOWN`; **`isConcurrencySafe` settle** · `extras.concludeTurn`/`deferContext` · `AgentCancelCause` · vision-exp catalog · turn-error after same-turn retry; SQLite/`toPackedJSONL` **`tool-call-chunks`**.

- **LLM 接线（Host） / LLM wiring (Host)**：`settings.yaml` + `.credentials.yaml` → `createSessionRoutingLlm`（每步按 live selection 解析 inner adapter，换模型无需 invalidate agent）；`session.selectModel` 经 `resolveProviderBinding`（Registry brand **或** `llm-pi-ai` 手写路由合成）；预检凭据 + 图像 modality；凭据/模型设置变更 invalidate agent cache。Agent loop 在 provider 调用前写 `request/header`（`foldRequestHeader` 可重建当前 route）；CLI `run`/`serve` 均读 workspace 设置（非仅 `XRK_LLM_PRESET`）。**Prompt cache（对齐对照行为）**：出站 tools 字典序；同 turn 后续 step 不再插入 `[current message]` / 变动 `time:`；workspace / skill 目录走持久 user inject（不砸 system 前缀）；对话前缀可复用（StatsLine「缓存命中」读 `tokenUsage.cacheReadTokens`）。**Think / reasoningEffort**：Face 选择的 `reasoningEffort` → routing/`LlmChatRequest` → DeepSeek 出站 `thinking` + `reasoning_effort`（`off` → `thinking.disabled`，不发 `reasoning_effort: off`）。 / `settings.yaml` + `.credentials.yaml` → `createSessionRoutingLlm` (each step resolves the inner adapter from the live selection; model switches do not require invalidating the agent); `session.selectModel` goes through `resolveProviderBinding` (Registry brand **or** `llm-pi-ai` handwritten-route synthesis); preflight credentials + image modality; credential/model setting changes invalidate the agent cache. The agent loop writes `request/header` before the provider call (`foldRequestHeader` can reconstruct the current route); CLI `run`/`serve` both read workspace settings (not only `XRK_LLM_PRESET`). **Prompt cache (aligned behavior)**: outbound tools sorted lexicographically; later steps in the same turn no longer insert `[current message]` / changing `time:`; workspace / skill catalogs use durable user inject (do not smash the system prefix); conversation prefixes are reusable (StatsLine “cache hit” reads `tokenUsage.cacheReadTokens`). **Think / reasoningEffort**: Face-selected `reasoningEffort` → routing/`LlmChatRequest` → DeepSeek outbound `thinking` + `reasoning_effort` (`off` → `thinking.disabled`, never `reasoning_effort: off`).

- **Registry 协议 / Registry protocols**：R0 `openai-chat`（`openai-completions` 同工厂别名）+ R1 `anthropic-messages` · `openai-responses` · `gemini-generate`；Face `llm-pi-ai.providers.*.api` → `resolveProviderBinding({ protocol })` 选工厂；品牌 `anthropic` / `gemini` / `openai-responses` 入 catalog；手写 id（Custom provider）不进 brand 表，由 Face 合成 binding。 / R0 `openai-chat` (`openai-completions` same-factory alias) + R1 `anthropic-messages` · `openai-responses` · `gemini-generate`; Face `llm-pi-ai.providers.*.api` → `resolveProviderBinding({ protocol })` picks the factory; brands `anthropic` / `gemini` / `openai-responses` enter the catalog; handwritten ids (Custom providers) stay out of the brand table and are synthesized by Face.

- **DeepSeek 默认模型 / DeepSeek default models**：Face catalog / `llm-deepseek` 预设 `deepseek-v4-flash` · `deepseek-v4-pro` · `deepseek-v4-flash-vision-exp`（官方 V4：1M context · 384K max output）；`agent-default-model` 默认 `deepseek-v4-flash`。 / Face catalog / `llm-deepseek` presets those three models (official V4: 1M context · 384K max output); `agent-default-model` defaults to `deepseek-v4-flash`.

- **模型发现 / Model discovery**：Face `llm.discoverModels` 对 `settingsNs` `llm` / `llm-pi-ai` 发 openai-chat `GET /models`（draft，不落盘）；失败 `model-discovery-failed` 且 details 不含密钥。 / Face `llm.discoverModels` issues openai-chat `GET /models` for `settingsNs` `llm` / `llm-pi-ai` (draft, not persisted); failures are `model-discovery-failed` with details that omit secrets.

- **设置文档 / Settings documents**：`~/.xrk/settings.yaml` + `.credentials.yaml` 为 Face 设置/凭据真源（`settings.update` · `credentials.set` 落盘）；`settings.openDocument` 打开该 yaml。`settings.describe` 带 schemastery 形 namespace（`llm-deepseek` · `llm-pi-ai` · `agent-presets` · `agent-default-model` · `permission` · `mcp` · `bash` · `agent-loop` · `web-search` 等）。`bash` / `agent-loop` / `web-search` 保存后 Host 重建 agent（timeout · 输出上限 · 并行 settle · toolSettle · llmRetryMaxRetries · 搜索提供方与密钥端到端）。Settings → Plugins 卡：`mcp` · `web-search` · `bash` · `agent-loop`（Web search 卡上可写 Tavily/Brave 密钥，经 Credentials 落盘；[web-tools.md](./web-tools.md)）。composer 模型目录读 settings 的 `models[]`（无 `default` 占位）；`session.selectModel` 写入 `agent-default-model`；工作区列表落 `~/.xrk/workspaces.json`；`mcp.servers` + `allowConnect` 与 `~/.xrk/host-settings.json` 热挂载并存。Settings → Plugins → MCP 可粘贴 Cursor `{ "mcpServers": { "name": { "command", "args" } } }`。 / `~/.xrk/settings.yaml` + `.credentials.yaml` are the Face settings/credentials source of truth (`settings.update` · `credentials.set` persist); `settings.openDocument` opens that yaml. `settings.describe` carries schemastery-shaped namespaces (`llm-deepseek` · `llm-pi-ai` · `agent-presets` · `agent-default-model` · `permission` · `mcp` · `bash` · `agent-loop` · `web-search`, etc.). Saving `bash` / `agent-loop` / `web-search` rebuilds the Host agent (timeout · output caps · parallel settle · toolSettle · llmRetryMaxRetries · search provider and keys end-to-end). Settings → Plugins cards: `mcp` · `web-search` · `bash` · `agent-loop` (Tavily/Brave keys may be entered on the Web search card and persist via Credentials; [web-tools.md](./web-tools.md)). The composer model directory reads settings `models[]` (no `default` placeholder); `session.selectModel` writes `agent-default-model`; the workspace list persists to `~/.xrk/workspaces.json`; `mcp.servers` + `allowConnect` coexist with hot-mount from `~/.xrk/host-settings.json`. Settings → Plugins → MCP accepts pasted Cursor `{ "mcpServers": { "name": { "command", "args" } } }`.

- **目录选择 / Directory picker**：`host.pickDirectory` 弹系统选文件夹；取消 `{ path: null }`；缺 zenity/kdialog 或非桌面平台 `directory-picker-unavailable`（不是假取消）。Win 走 PowerShell STA `FolderBrowserDialog`（单测注入 picker，不弹框）。 / `host.pickDirectory` opens the system folder picker; cancel returns `{ path: null }`; missing zenity/kdialog or non-desktop platforms return `directory-picker-unavailable` (not a fake cancel). Windows uses PowerShell STA `FolderBrowserDialog` (tests inject a picker; no dialog).

- **斜杠 / Slash commands**：`session.prompt` 已知命令回 `{ command: { kind: "success" } }`；未知 `/name` 当普通 prompt 入账（壳 Zod 不容 `kind: "error"`），再由 `assemble.resolveSlash` 展开 recipe 或把 skill 全文写入 `user/message`（recipe 同名优先；不进 `commands/list`）。失败已知命令 `command-error`。内建 `/compact` 在 idle 时写 `context/compaction`（`reason: manual`），无历史则 success「No compactable history yet.」。内建 `/export` 只入账「Session log download requested.」（ZIP 仍走 `GET/HEAD /api/session.export`）。内建 `/feedback` 写 `feedback/record`（log-only；`command/run` 不落 `args`）；无分享后端，成功文案为「Session sharing is not configured.」。 / `session.prompt` returns `{ command: { kind: "success" } }` for known commands; unknown `/name` is booked as a normal prompt (shell Zod rejects `kind: "error"`), then `assemble.resolveSlash` expands a recipe or writes the full skill text into `user/message` (same-named recipe wins; not listed in `commands/list`). Failed known commands are `command-error`. Built-in `/compact` writes `context/compaction` (`reason: manual`) when idle; with no history it succeeds with “No compactable history yet.”. Built-in `/export` only books “Session log download requested.” (ZIP still via `GET/HEAD /api/session.export`). Built-in `/feedback` writes `feedback/record` (log-only; `command/run` does not persist `args`); without a sharing backend the success copy is “Session sharing is not configured.”.

- **会话搜索 / Session search**：`session.search` 扫 user/assistant/admit/safety/command/todo/feedback，按最近活动排序；持久化仓走 **FTS5 trigram** 候选 + 事件拼 snippet；内存仓全扫。 / `session.search` scans user/assistant/admit/safety/command/todo/feedback, sorted by recent activity; persistent stores use **FTS5 trigram** candidates plus event-built snippets; in-memory stores full-scan.

- **预设只读 / Presets read-only**：`agentPreset.read` 返回 catalog markdown；copy/remove/openDocument 回 `agent-preset-read-only`（`authorable: false`）。 / `agentPreset.read` returns catalog markdown; copy/remove/openDocument return `agent-preset-read-only` (`authorable: false`).

- **消息反馈 / Message feedback**：`messageFeedback/list/put/delete` 进程内 CAS（不写 session 日志）；`messageId` = `{turnId}:{stepId}`。 / `messageFeedback/list/put/delete` are in-process CAS (do not write the session log); `messageId` = `{turnId}:{stepId}`.

- **Goal**：`/goal` 或 `goals/create`（及 DSH 点号 `goal.create`）写入投影 `goal` 并 admit；pause/resume/edit/clear 走 CAS；`turn/end` 在 `armed` 时续轮，满 `maxGoalRounds`（默认 16）则 `blocked`。Host 旁路 `{XRK_SESSIONS_DIR}/goals.json`。不是独立 Goal fiber。 / `/goal` or `goals/create` (and DSH dotted `goal.create`) write projection `goal` and admit; pause/resume/edit/clear use CAS; `turn/end` continues while `armed`, and becomes `blocked` at `maxGoalRounds` (default 16). Host sidecar `{XRK_SESSIONS_DIR}/goals.json`. Not a standalone Goal fiber.

- **Cordis 面板 / Cordis panel**：`dynamicCordisRunner/*` 由 Face `cordis-stub` 转发 dsh-compat（可选 `cordis-fiber-runner` 子进程）；不嵌 Cordis Host 主进程。 / `dynamicCordisRunner/*` is forwarded by Face `cordis-stub` to dsh-compat (optional `cordis-fiber-runner` subprocess); the Cordis Host main process is not embedded.

- **会话导出 / Session export**：`HEAD/GET /api/session.export?sessionId=` 返回 ZIP（`sessions/{id}.jsonl` 为 `toPackedJSONL` + 同名 `.jsonl.zst` + 子会话 + 附件；manifest 标 `sessionEncoding` / `sessionCompressedSidecar`）。 / `HEAD/GET /api/session.export?sessionId=` returns a ZIP (`sessions/{id}.jsonl` as `toPackedJSONL` + same-name `.jsonl.zst` + child sessions + attachments; manifest marks `sessionEncoding` / `sessionCompressedSidecar`).

- **MCP**：stdio + streamable-http → `mcp__*` 工具；默认 `mcp.connect` deny；Host 经 `XRK_MCP_*` 接线，空 env 时读 `~/.xrk/host-settings.json` 的 `mcp.servers`（也接受 Cursor `{ mcpServers }`）并在 Face mutate 后 `reconcileMcpToolPlugins` 热挂载（`applies: live`；deny → **park**，真实失败才 `connectFailures`）；HTTP 转发 SDK `reconnectionOptions`；stdio/**HTTP** 均在首次成功 connect 后有界进程重连（稳定窗口 = `maxDelayMs`；帽满 / disabled → gave-up 卸工具）；health 变推 `settings/document-updated`。壳 **Settings → Plugins → MCP** 卡可粘贴该 JSON；硬刷 `product-shell-mcp.e2e.ts`。 / stdio + streamable-http → `mcp__*` tools; default `mcp.connect` deny; Host wires via `XRK_MCP_*`, and with empty env reads `mcp.servers` from `~/.xrk/host-settings.json` (also accepts Cursor `{ mcpServers }`) then `reconcileMcpToolPlugins` hot-mounts after Face mutate (`applies: live`; deny → **park**; real failures become `connectFailures`); HTTP forwards SDK `reconnectionOptions`; both stdio/**HTTP** do bounded process reconnect after the first successful connect (stable window = `maxDelayMs`; cap / disabled → gave-up and unload tools); health changes push `settings/document-updated`. The shell **Settings → Plugins → MCP** card accepts that JSON; hard-refreshed by `product-shell-mcp.e2e.ts`.

- **工具卡 / Tool cards**：工具声明 `presentCall` / `presentResult`；Face mux/history `viewFor` 只 lookup（抛错 / 缺 pairing / 无 presenter → 没 view，壳走 generic）。Host 把 preset standing 工具表交给 Face（冷 history 不 wake agent；DSH `ctx.tools.get` 的 standing 层）。mux/history `tool/call.arguments` 为 JSON 字符串（DSH `argsRaw`）。`session.history` 冷回放：`assistant/message.reasoning` → wire `content` 首块 `type: "reasoning"`；bash / `terminal_*` 走 standing presenter。`bash` 模型文本用 DSH `[exit code: N]`。不写 session 日志；history 用同一次扫描的 call pairing。浏览器硬刷 Host-serve 车道已勾（Think / 工具卡 / 审批 / 提问 / TodoDock / Access / Plan / plan-review / Session log 导出 / inventory）；DSH Cordis scaffold 金标未搬（产品差异）。 / Tools declare `presentCall` / `presentResult`; Face mux/history `viewFor` is lookup-only (throw / missing pairing / no presenter → no view; shell uses generic). Host hands the preset standing tool table to Face (cold history does not wake the agent; standing layer of DSH `ctx.tools.get`). mux/history `tool/call.arguments` is a JSON string (DSH `argsRaw`). Cold `session.history` replay: `assistant/message.reasoning` → first wire `content` block `type: "reasoning"`; bash / `terminal_*` use standing presenters. `bash` model text uses DSH `[exit code: N]`. Does not write the session log; history uses call pairing from the same scan. Browser hard-refresh Host-serve lanes covered (Think / tool cards / approval / questions / TodoDock / Access / Plan / plan-review / Session log export / inventory); DSH Cordis scaffold gold marks are a product difference (not ported).

- **附件投影 / Attachment projection**：有 AttachmentStore 时 Face 登记 DSH `imageLimits`（常量；history 尾页 / 列表基线；不推变更帧）。壳 InputBar 用它做粘贴/拖放预检；无仓则键缺席。 / With an AttachmentStore, Face registers DSH `imageLimits` (constant; history last page / list baseline; no change frames). The shell InputBar uses it for paste/drop preflight; without a store the key is absent.

- **外壳 / 内核 / Shell / kernel**：产品壳 = `apps/web` + `packages/client`（不是 GitHub Fork）。品牌 `apps/web/public`；Hero 标语「向阳而生，驭光而行」。serve 托管 `apps/web/dist`（`web:build` + `client:bundle` + `web:assemble`）。裸 Vite `serve`/`dev` 在 config 钩子拒绝（指向 `xrkh web`，不先 `listen`）。Host `applyXrkProductBootPolicy` 去 Cordis UI/HMR。对接层 = `packages/server/face/src/wire/`；内核 = session · 工具瀑布 · compose。`apps/console` 为 Face 验证台（`?console=1`），非产品入口。Host 测：有完整 dist（index + boot + plugins，含 Face 立即层三件）才跑壳首屏，否则 skip。欢迎窗 / 侧栏 / 流式 / 工具卡 / 审批 / 提问 / inventory / Think / TodoDock / Access / Plan / plan-review / Session log 导出硬刷走 `pnpm test:web`（不进 `pnpm check`）。 / Product shell = `apps/web` + `packages/client` (not a GitHub Fork). Brand assets in `apps/web/public`; Hero line “向阳而生，驭光而行”. serve hosts `apps/web/dist` (`web:build` + `client:bundle` + `web:assemble`). Bare Vite `serve`/`dev` is rejected in config hooks (points to `xrkh web`, does not `listen` first). Host `applyXrkProductBootPolicy` strips Cordis UI/HMR. Integration layer = `packages/server/face/src/wire/`; kernel = session · tool waterfall · compose. `apps/console` is the Face verification console (`?console=1`), not the product entry. Host tests run the shell first paint only with a complete dist (index + boot + plugins, including the three Face immediate-layer packages); otherwise skip. Welcome / sidebar / streaming / tool cards / approval / questions / inventory / Think / TodoDock / Access / Plan / plan-review / Session log export hard-refresh via `pnpm test:web` (not part of `pnpm check`).

- **CLI**：主 bin **`xrkh`**（亦 `xrk-harness`）：`run | serve | web | plugin | doctor | dump-config`。`serve`/`web` 托管产品壳（按 CLI 包定位）；`--host`/`--port`/`--open`；拒绝 `0.0.0.0`。`run` 认 `XRK_LLM_PRESET`、位置参数与 stdin prompt。 / Primary bin **`xrkh`** (also `xrk-harness`): `run | serve | web | plugin | doctor | dump-config`. `serve`/`web` host the product shell (resolved from the CLI package); `--host`/`--port`/`--open`; rejects `0.0.0.0`. `run` accepts `XRK_LLM_PRESET`, positional args, and stdin prompt.

- **运行时 / Runtime**：Node ≥ 26。 / Node ≥ 26.

细节 / Details：[architecture.md](./architecture.md) · [getting-started.md](./getting-started.md) · [configuration.md](./configuration.md) · [host-face.md](./host-face.md) · [community-plugins.md](./community-plugins.md) · [plugin-loader.md](./plugin-loader.md) · [policy.md](./policy.md) · [modules/](./modules/README.md)
