# Compose C0 Implementation Plan

> **For agentic workers:** Inline execution in this session (user: 接着 do).

**Goal:** Ship `@xrkseek/compose` leaf with Scope / effect / provide / inject / Ordering / isolate, tests, docs — no Host wiring.

**Architecture:** Explicit Scope tree + realm-keyed binding registry; LIFO effects; dispose inertia; dependents unload before provide withdrawal. No Proxy / Cordis / Effect-TS. Zero runtime deps in C0 (`ServiceKey` local).

**Tech Stack:** TypeScript · Vitest · pnpm workspace

## Global Constraints

- ADR-0002 / 0004 / 0005 Accepted
- No `kernel` → `compose`; C0 no Host/Face wiring
- `pnpm check` must pass

---

### Task 1: Scaffold + core Scope

**Files:**
- Create: `packages/compose/package.json`, `tsconfig.json`, `README.md`, `src/*.ts`, `tests/compose.test.ts`
- Modify: `tsconfig.json`, `vitest.config.ts`, `AGENTS.md`, `docs/architecture.md`, `docs/status.md`, ADR/spec status

- [x] Implement + test C0 in one pass (user asked 接着 do)
- [x] `pnpm check` green

**Produces:** `createRootScope`, `Scope`, Ordering/Pending/isolate invariants
