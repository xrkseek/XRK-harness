# v0.1.26

> **读者 / Audience**：终端用户 · 维护者（发行说明） / End users · Maintainers (release notes)

相对 [v0.1.25](./v0.1.25.md)：修复设置字号空白；完善下游产品 skill 剧本。

Relative to [v0.1.25](./v0.1.25.md): fix blank Font size in Settings; improve downstream product skill playbooks.

---

## 修复 / Fixed

* 修复 Settings **Font size** 显示空白：Face `ui-theme` schema / describe base 补齐 `fontSize`（默认 14）；客户端采用缺省时回退默认值，不再把 `undefined` 画进步进器

---

## 完善 / Improved

* 完善 CLI 种子 skill（`xrk-capability-attach` · `xrk-create-skill` · `xrk-adapt-workspace`）：第三人称 description、清单式流程、Playwright MCP 示例；去掉日记腔
* 完善 Face `fontSize` 校验为单一辅助函数，避免双份范围检查

---

## 安装 / Install

```bash
npm install -g @xrkseek/harness-cli@0.1.26
xrkh web
```

或 / or：`npm i -g @xrkseek/harness-cli@latest`

### 源码维护者

```bash
pnpm install && pnpm build
pnpm web:build && pnpm client:bundle && pnpm web:assemble
pnpm check
pnpm release
```
