# v0.4.5

> **读者 / Audience**：终端用户 · 插件与集成者 · 维护者（发行说明）

相对 [v0.4.4](./v0.4.4.md)：修复壳层因 fork 会话导致侧栏会话列表整表空白。

Relative to [v0.4.4](./v0.4.4.md): restore the sidebar session list when any forked session is present.

### 修复

* 修复 Face 返回 `origin: "fork"` 时，客户端 `session.list` Zod 只认 `"subagent"`，整表解析失败，侧栏工作区下会话记录全部不显示（磁盘库未丢）。

### 其他变更

* 无。

---

### Fixed

* Fix client `session.list` Zod rejecting `origin: "fork"` (only `"subagent"` was allowed), which failed the whole list parse and emptied the sidebar under every workspace while `sessions.db` still held the data.

### Other changes

* None.
