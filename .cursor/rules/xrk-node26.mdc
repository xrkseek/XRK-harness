---
description: 本机与 Agent 必须用系统 Node ≥26；禁止 Cursor 自带 Node 22
alwaysApply: true
---

# Node ≥26（工具链真源）

仓库 `engines.node`：**`>=26`**。根 `packageManager` 钉死 pnpm（当前见根 `package.json`）。

## 用哪份 node

| 来源 | 路径特征 | 可否用 |
|------|----------|--------|
| 系统安装 | `C:\Program Files\nodejs\node.exe`（或同版在 PATH 最前） | **必须** |
| Cursor helper | `…\cursor\resources\app\resources\helpers\node.exe`（常为 **22**） | **禁止** |

Agent / 本机 shell 跑 `pnpm` · `tsc` · `vitest` · `pnpm check` · `pnpm release` 前：

```powershell
$env:Path = "C:\Program Files\nodejs;" + $env:Path
node -v   # 须 ≥26
```

若 `where.exe node` 先命中 Cursor helper，或 `node -v` 显示 22.x：**先改 PATH，再执行**，不要带着 22 过门禁。

## pnpm

- 本机：`npm install -g pnpm@<根 packageManager 精确版>`
- **不用** Corepack 升版 / 启 pnpm
- CI：`pnpm/action-setup` 跟随根 `packageManager`

## 勿写进教科书

本条是 **笔记红线**（Agent / 维护者）。用户说明书只写「需要 Node ≥26」，不写 Cursor helper 路径。
