---
description: 本机与 Agent 必须用系统 Node ≥26；禁止 Cursor 自带 Node 22
alwaysApply: true
xrk-inject: false
---

# Node ≥26（工具链真源）

仓库 `engines.node`：**`>=26`**。根 `packageManager` 钉死 pnpm（当前见根 `package.json`）。

## 用哪份 node

| 来源 | 识别方式 | 可否用 |
|------|----------|--------|
| Cursor helper | 路径含 `cursor/resources/app/resources/helpers/node.exe`（常为 **22.x**） | **禁止** |
| 系统安装 | `where.exe node`（Windows）或 `which -a node`（Unix）里**非 helper** 的条目 | **必须**，且 `node -v` ≥ 26 |

**勿写死盘符或目录**（例如 `C:\Program Files\nodejs`）。Node 可能在 C/D/E 盘、nvm、fnm、Homebrew 等；以**当前机器**探测为准。

Agent / 本机 shell 跑 `pnpm` · `tsc` · `vitest` · `pnpm check` · `pnpm release` 前：

```powershell
# Windows — 选 PATH 上第一个非 Cursor helper 的 node，把它所在目录 prepend 到 PATH
$pick = @(where.exe node 2>$null) | Where-Object {
  $_ -notmatch '\\cursor\\resources\\app\\resources\\helpers\\node\.exe$'
} | Select-Object -First 1
if (-not $pick) { throw 'No system Node on PATH' }
$env:Path = "$(Split-Path -Parent $pick);$env:Path"
node -v   # 须 ≥26
```

```bash
# macOS / Linux — 若 `command -v node` 命中 Cursor helper，改用 PATH 上下一个 node
node -v   # 须 ≥26；若为 22.x 或路径含 cursor/.../helpers/node，换系统 Node 再试
```

若 `node -v` 仍为 22.x：**先修正 PATH / 安装 Node ≥26**，不要带着 22 过门禁。

## pnpm

- 本机：`npm install -g pnpm@<根 packageManager 精确版>`
- **不用** Corepack 升版 / 启 pnpm
- CI：`pnpm/action-setup` 跟随根 `packageManager`

## 勿写进教科书

本条是 **笔记红线**（Cursor 里改本仓的 Coding Agent）。用户说明书只写「需要 Node ≥26」，不写 Cursor helper 路径，也不写某台机器的盘符。
