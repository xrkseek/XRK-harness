---
description: Face → client wire：枚举/字段同批改 Zod；session.list 按行容错
globs: packages/server/face/**/*,packages/stubs/xrk-host-apiproxy/**/*,packages/client/runtime/src/client/sessions/**/*
alwaysApply: false
xrk-inject: false
---

# 笔记 · Face / client wire

扩展 Host→client 字段（`session.list` · `host/session-added` 等）时**同一变更**改齐：

1. Face 发射（`packages/server/face`）
2. apiproxy 类型 + Zod（`packages/stubs/xrk-host-apiproxy/src/api/*`）
3. Client 消费（若依赖该字段）
4. 测：至少覆盖新枚举值（见 `session-list-schema.test.ts`）

**列表基线**：`session.list` 的 `items[]` 按行 `safeParse`；坏行 drop + warn，禁止整表因一行挂掉（对齐 SSE 坏帧跳过）。可选枚举用 `sessionOriginSchema`（未知 → `undefined`）。

事故对照：v0.4.0-rc.1 起 Face 发 `origin: "fork"`，Zod 只认 `subagent` → 侧栏空；v0.4.5 补枚举 + 按行容错。
