---
description: 发行说明文体 · 新增/完善/删除/修复 · 禁止日记腔
xrk-inject: false
globs: docs/releases/**/*.md
alwaysApply: false
---

# 发行说明（`docs/releases/`）

本仓四类 + 可选「其他」：`新增` / `完善` / `删除` / `修复`（+ `其他变更`）。结构对齐常见开源 Release 归类，不以第三方产品说明为基线。

## 版本号（先读）

`MAJOR.MINOR.PATCH`：

| 位 | 名称 | 本仓用途 |
|----|------|----------|
| 第二位 `MINOR` | 次版本号 | **奇 = 正式线**（`0.3.x`），**偶 = 预览线**（`0.2.x`） |
| 第三位 `PATCH` | 修订号 | **同一线上顺序 +1**（`0.3.7` 后下一正式补丁是 `0.3.8`，**不是**跳到 `0.3.9`） |

禁止把 PATCH 奇偶当成正式/预览。真源表：[docs/releases/README.md](../../docs/releases/README.md) · [docs/publishing.md](../../docs/publishing.md)。

## 结构

```markdown
# vX.Y.Z

> **读者**：终端用户 · 维护者（发行说明）。

相对 [vA.B.C](./vA.B.C.md)：一行总览（用户能感知的变化）。

### 新增
* …

### 完善
* …

### 删除
* …（无删除可省略本节）

### 修复
* …

### 其他变更
* …（破坏性、工具链、发包边界；无则省略）

## 安装
…（`npx` / Packages / Release tgz）
```

GitHub Release body 与 `docs/releases/v*.md` **同构**；中文为主（不必强制双语）。

## 每条怎么写

- **一条一个用户可感知结果**；动词起头：新增 / 支持 / 完善 / 优化 / 移除 / 修复 / 修正。
- 写清**对象 + 结果**（「修复取消流式后回复前缀未带入后续提问」），不写过程流水账。
- 破坏性变更放「其他变更」，一句点明不兼容点。
- 安装与验证可附在文末；验证表只列本版相关项。

## 禁止

- 日记腔：「今天」「本周」「我们顺便」「继续推进」
- 按内部域堆标题当主结构（`## Session` / `## CI` 可作次要，**不能替代** 新增/完善/删除/修复）
- PR / commit 列表当正文；未合并草稿口吻
- 空泛：「若干优化」「修了一些 bug」「重构代码」

## 对照（差 → 好）

| 差 | 好 |
|----|----|
| 继续打磨 MCP 设置 | 完善 MCP 设置卡：竖排名称/传输/命令/参数/cwd，与 Agent 卡同构 |
| CI 相关调整 | 修复 CI：`pnpm/action-setup` 跟随根 `packageManager`，不再写死 v9 |
| 清理了一些旧代码 | 删除冗余 `FACE_LLM_SCHEMA` 与泛化 `llm` settings mutate 路径 |
